def get_palette(num_cls):
    """ Returns the color map for visualizing the segmentation mask.
    Args:
        num_cls: Number of classes
    Returns:
        The color map
    """
    custom_palette = [255, 255, 255, 0, 0, 0, 0, 0, 0, 0, 0, 0]
    return custom_palette

